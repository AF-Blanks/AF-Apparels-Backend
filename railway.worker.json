{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "celery -A app.core.celery worker --loglevel=info -Q celery,default,quickbooks,email,pricelist,inventory --concurrency=4",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
